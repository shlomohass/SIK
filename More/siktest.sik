let a, b, c;

print 1 + 2;


// { name : "sik" }.name; <--- HANDLE internal error
/*
while (true) { continue; }
if (test) { breakif; }
*/

//each (a, i, e) { test = test + 1; }
//each (a, e) { print "yes"; }
//for (j, j < 10) ~{ --j; j += 2; }
